// Web stub: react-native-maps is native-only.
// ExpoMap falls back to LeafletMap on web, so this stub is never called.
export default null;
export const Marker = null;
export const PROVIDER_GOOGLE = null;
export const UrlTile = null;
